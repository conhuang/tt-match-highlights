# future features
1. upload failed should indicate that it failed. instead it currently says "uploading"
2. Orphaned S3 Multipart Upload Chunks: When a multipart upload is initiated but interrupted (and never completed or aborted), S3 retains those 50MB chunk files in hidden storage indefinitely.
3. match metadata (like player) should be editable
4. UI should be more helpful, with instructions and clear that this is for editing and analyzing full matches 
